# [페이지명] 페이지

> **경로**: `/route`
> **파일**: `src/app/.../page.tsx`
> **복잡도**: ★★★☆☆ (중간)
> **최종 업데이트**: YYYY-MM-DD

---

## 개요

### 목적
[이 페이지가 해결하는 문제 / 제공하는 가치를 1-2문장으로 설명]

### 주요 기능
- [ ] 기능 1
- [ ] 기능 2
- [ ] 기능 3

---

## 의존성

### 사용하는 훅
| 훅 | 파일 | 용도 |
|----|------|------|
| `useXxx` | `useXxx.ts` | 데이터 조회 |
| `useYyyMutation` | `useYyy.ts` | 데이터 수정 |

### 사용하는 컴포넌트
| 컴포넌트 | 용도 |
|----------|------|
| `<ComponentA />` | [용도] |
| `<ComponentB />` | [용도] |

---

## 상태 관리

### 로컬 상태
| 상태 | 타입 | 초기값 | 설명 |
|------|------|--------|------|
| `loading` | `boolean` | `true` | 로딩 상태 |
| `items` | `Item[]` | `[]` | 목록 데이터 |

### 서버 상태 (React Query)
| 쿼리 키 | 데이터 | 갱신 시점 |
|---------|--------|----------|
| `['key', id]` | `DataType` | 마운트 시 |

---

## UI 구성

### 레이아웃
```
┌────────────────────────┐
│       Header           │
├────────────────────────┤
│                        │
│      Main Content      │
│                        │
├────────────────────────┤
│       Footer/FAB       │
└────────────────────────┘
```

### 컴포넌트 트리
```
Page
├── Header
├── ContentSection
│   ├── ItemCard
│   └── EmptyState (조건부)
└── FloatingActionButton (조건부)
```

### 상태별 UI
| 상태 | 표시 내용 |
|------|----------|
| 로딩 | `<Skeleton />` 또는 스피너 |
| 에러 | `<ErrorState message={...} />` |
| 빈 상태 | `<EmptyState />` |
| 정상 | 메인 컨텐츠 |

---

## 비즈니스 로직

### 핵심 로직
1. **[로직명]**: [설명]
2. **[로직명]**: [설명]

### 검증 규칙
- [규칙 1]
- [규칙 2]

### 사용자 흐름
```
1. 페이지 진입
2. 데이터 로드
3. 사용자 인터랙션
4. 결과 확인
```

---

## API / 데이터

### 사용하는 테이블
| 테이블 | 용도 | RLS |
|--------|------|-----|
| `table_name` | [용도] | 예/아니오 |

### 주요 쿼리 패턴
```typescript
// 예시
const { data } = useQuery({
  queryKey: ['key', id],
  queryFn: () => fetchData(id)
});
```

---

## 접근 권한

| 사용자 유형 | 접근 가능 | 비고 |
|-------------|----------|------|
| 비로그인 | X | 로그인 리다이렉트 |
| 일반 사용자 | O | - |
| 관리자 | O | 추가 기능 표시 |

---

## 관련 문서

- [관련 페이지 1](./related-page.md)
- [아키텍처 가이드](../../ARCHITECTURE_GUIDE.md)

---

## 변경 이력

| 날짜 | 변경 내용 | 관련 커밋 |
|------|----------|----------|
| YYYY-MM-DD | 최초 작성 | - |
